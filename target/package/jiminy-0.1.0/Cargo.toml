# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "jiminy"
version = "0.1.0"
authors = ["MoonManQuark"]
build = false
exclude = [
    ".github",
    "*.sh",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Anchor-style safety checks for pinocchio programs — zero-copy, no_std, BPF-safe"
homepage = "https://github.com/BluefootLabs/jiminy"
readme = "README.md"
keywords = [
    "solana",
    "pinocchio",
    "bpf",
    "zero-copy",
    "anchor",
]
categories = [
    "no-std",
    "embedded",
]
license = "Apache-2.0"
repository = "https://github.com/BluefootLabs/jiminy"

[lib]
name = "jiminy"
path = "src/lib.rs"

[dependencies.pinocchio]
version = "0.10"
features = ["cpi"]

[dependencies.pinocchio-pubkey]
version = "0.3"
