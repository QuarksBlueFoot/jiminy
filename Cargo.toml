[workspace]
members = [
    ".",
    "examples/jiminy-vault",
    "examples/jiminy-escrow",
    "bench/pinocchio-vault",
    "bench/jiminy-vault",
    "bench/anchor-vault",
]
exclude = [
    "bench/runner",
]
resolver = "2"

[workspace.lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(target_os, values("solana"))','cfg(target_arch, values("bpf"))'] }

[workspace.dependencies]
pinocchio = { version = "0.10", features = ["cpi"] }
pinocchio-pubkey = "0.3"
jiminy = { path = "." }

[package]
name = "jiminy"
version = "0.3.1"
edition = "2021"
license = "Apache-2.0"
authors = ["MoonManQuark"]
description = "Complete safety toolkit for pinocchio Solana programs. Account checks, token/mint readers, Token-2022 extension screening, CPI reentrancy guards, DeFi math (u128), slippage protection, time/deadline checks, state machines, PDA utilities. Zero-copy, no_std, no proc macros."
repository = "https://github.com/QuarksBlueFoot/jiminy"
homepage = "https://github.com/QuarksBlueFoot/jiminy"
readme = "README.md"
keywords = ["solana", "pinocchio", "bpf", "zero-copy", "anchor"]
categories = ["no-std", "embedded"]
exclude = [".github", "*.sh", "examples", "bench", "docs", "BENCHMARKS.md"]

[lib]
name = "jiminy"

[dependencies]
pinocchio = { workspace = true }
pinocchio-pubkey = { workspace = true }

[features]
default = ["programs"]
programs = []

[lints]
workspace = true
