[package]
name = "jiminy"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
authors = ["MoonManQuark"]
description = "Anchor-style safety checks for pinocchio programs — zero-copy, no_std, BPF-safe"
repository = "https://github.com/QuarksBlueFoot/jiminy"
homepage = "https://github.com/QuarksBlueFoot/jiminy"
readme = "README.md"
keywords = ["solana", "pinocchio", "bpf", "zero-copy", "anchor"]
categories = ["no-std", "embedded"]
exclude = [".github", "*.sh"]

[lib]
name = "jiminy"

[dependencies]
pinocchio = { version = "0.10", features = ["cpi"] }
